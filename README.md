# vine-seg_segmentation
## Installation
```python
# install dependencies from requirements.txt
pip install -r requirements.txt

# install detectron2 from source
pip install 'git+https://github.com/facebookresearch/detectron2.git'
```
